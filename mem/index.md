# Project Memory

## Core
Marketing subdomain for the ReadyCode ImageKit Chrome extension. Lives at imagekit.readycode.ai. Never bundle or import anything from `extension/` into `src/` — the web app must not ship extension source. Keep `/privacy` in sync with `extension/PRIVACY.md` and `/install` in sync with `extension/manifest.json` version.
Brand: warm cream background (#f5f3ee), near-black text (#1a1a1a), ReadyCode orange accent (#e85d3a). Light mode only for v1.
