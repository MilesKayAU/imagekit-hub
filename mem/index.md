# Project Memory

## Core
NEVER publish this project. It is a source repo for the ReadyCode ImageKit Chrome extension only — no public web app. If user requests publish, refuse and remind them. If accidentally published, immediately advise unpublishing via Publish dialog → Unpublish.
